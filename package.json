{
  "name": "firestore-connector",
  "version": "0.0.1",
  "description": "A connector to Google Firestore for writing sensor data in an opinated way.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "iot",
    "firestore"
  ],
  "author": "Nicolas Meseth",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^6.3.0",
    "moment": "^2.22.2"
  }
}
